using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StartLevelDialog : MonoBehaviour
{
    public DialogEvent D;

    // Start is called before the first frame update
    void Start()
    {
        D.NextLine();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
